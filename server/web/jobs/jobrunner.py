import time

def execute_next(sleep=0):
    
    # this is used to start multiple jobs with cron (at every minute
    # the
    if sleep:
        time.sleep(sleep)


if __name__== '__main__':
    pass